# =============================================================================
# OTIN CARWASH — isi .env untuk shared hosting cPanel (ArenHost)
#
# Salin isi berkas ini menjadi:  ~/otin-carwash/.env  (BUKAN di public_html)
#
# Yang bertanda <...> WAJIB diganti. Jangan pernah commit .env yang sudah
# terisi ke git, dan jangan kirim isinya lewat WhatsApp/email.
# =============================================================================

APP_NAME="OTIN CARWASH"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://otincarwash-pos.my.id

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=warning

# --- Database -----------------------------------------------------------
# Dibuat di cPanel -> MySQL Databases. cPanel MENAMBAHKAN prefix username
# akun di depan nama database & user, mis. "otincarw_otin". Salin persis
# yang tampil di cPanel, jangan ditebak.
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=<prefix>_otin_carwash
DB_USERNAME=<prefix>_otin
DB_PASSWORD=<password-database>

SESSION_DRIVER=database
SESSION_LIFETIME=840
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# Sudah HTTPS di hosting: paksa cookie sesi hanya lewat HTTPS.
SESSION_SECURE_COOKIE=true

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

MAIL_MAILER=log
MAIL_FROM_ADDRESS="hello@otincarwash-pos.my.id"
MAIL_FROM_NAME="${APP_NAME}"

# --- Kredensial awal owner ----------------------------------------------
# Ini kredensial LOGIN PERTAMA saja. Begitu aplikasi jalan, ganti dari dalam
# aplikasi (menu -> Pengaturan -> Akun owner); sejak itu password tersimpan
# ter-hash di database dan baris di bawah tidak dipakai lagi.
#
# Sekarang aplikasi ada DI INTERNET, bukan LAN toko lagi: password 4 digit
# akan ditebak dalam hitungan menit. Minimal 12 karakter acak.
OWNER_USERNAME=<username-owner>
OWNER_PASSWORD=<password-owner-minimal-12-karakter-acak>

GEMINI_API_KEY=<key-baru-dari-google-ai-studio>
GEMINI_MODEL=gemini-3.5-flash

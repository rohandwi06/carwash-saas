# .env terisi per cucian: password DB, password awal owner, APP_KEY
secrets/
# paket hasil New-Release.ps1 (berisi vendor/, besar, bisa dibangun ulang dari commit)
releases/
# unduhan backup database cucian - data klien, bukan kode
backups/
*.zip
*.sql
*.sql.gz
